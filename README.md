# ogame-scores-logger
Using OgameAPI fetch player's scores every hour and serve it as a html server. Powered by Node.js.

### 1. Fisrt run
  ```
  npm install
  ```
  ```
  npm start
  ```
  or 
  ```
  npm start -- --offline
  ```
